export interface Standing {
  name: string;
  logo: string;
  position: string;
  ranking: string;
  points: string;
  forms: string[];
}

export interface FixturesAndResults {
  day: string;
  title: string;
  url: string;
}

export interface Club {
  name: string;
  logo: string;
  url: string;
}
